matrix:
  include:
    - language: c
      sudo: required
      services:
        - docker
      script:
        - docker build -t swiftcommandline .

    - language: swift
      script:
        - swift test
        - swift build
